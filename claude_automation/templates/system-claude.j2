# System-Level CLAUDE.md

This file provides Claude Code with comprehensive information about all available tools and utilities on this NixOS system.

*Last updated: {{ timestamp.strftime('%Y-%m-%d %H:%M:%S') }}*

## IMPORTANT: CLAUDE CODE TOOL SELECTION POLICY

**SYSTEM OPTIMIZATION LEVEL: EXPERT**
{% include 'shared/policies.j2' %}

{% include 'shared/command_examples.j2' %}

## System Information

- **OS**: NixOS (nixos-unstable)
- **Desktop**: GNOME (Wayland)
- **Shell**: Fish (with smart command substitutions)
- **Terminal**: Kitty (optimized for AI development)
- **Package Management**: Nix Flakes + Home Manager

## Available Tools

{% for category, tools in tool_categories.items() %}
{% if category.value == 'AI & MCP Tools' %}
### 🤖 {{ category.value }}

**This system is optimized for AI-assisted development with cutting-edge tools:**

{% for tool in tools %}
- **`{{ tool.name }}`** - {{ tool.description }}{% if tool.url %} ({{ tool.url }}){% endif %}

{% endfor %}
{% elif category.value == 'Modern CLI Tools' %}
### ⚡ {{ category.value }}

**ALWAYS use these modern alternatives instead of traditional POSIX commands:**

{% for tool in tools %}
- `{{ tool.name }}` - {{ tool.description }}{% if tool.url %} - {{ tool.url }}{% endif %}
{% endfor %}

{% elif category.value == 'System Support Packages' %}
### 📦 {{ category.value }}

**These are system libraries, fonts, and build dependencies (not directly invokable):**

{% for tool in tools %}
- `{{ tool.name }}` - {{ tool.description }}{% if tool.url %} - {{ tool.url }}{% endif %}
{% endfor %}

{% else %}
### {{ category.value }}
{% for tool in tools %}
- `{{ tool.name }}` - {{ tool.description }}{% if tool.url %} - {{ tool.url }}{% endif %}
{% endfor %}

{% endif %}
{% endfor %}

{% include 'shared/fish_abbreviations.j2' %}

## Maintenance

This file is automatically maintained and should be updated when:
- New packages are added to `nixos-config/modules/core/packages.nix`
- New home-manager programs are configured
- Fish shell abbreviations or functions are modified
- Development tools or workflows change

To regenerate this file, run the NixOS rebuild process which automatically updates Claude Code configurations.